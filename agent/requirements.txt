blessed>=1.20.0
requests>=2.31.0
python-dotenv>=1.0.0

# Optional — only needed for the web UI server (python pipeline.py)
flask>=3.0.0
flask-cors>=4.0.0
# Keycloak token verification for the web UI
PyJWT[crypto]>=2.8.0
